Step 1 : 於上方工具列 [ View ] 中 開啟 [ Resouce View ]Step 2 : 於 [ Resouce View ] 視窗中找出要增加 "拖曳檔案" 之 Dialog 並開啟Step 3 : 於 [ Properties ] 視窗中按下 [ Messages ] 按鈕Step 4 : 找出 [ OnDropFiles() ] 並增加Step 5 : 在 [ OnDropFiles() ] 中增加下列程式碼 (因個人程式需求而改變)TCHAR cFileName[256]; UINT uFileCount = 0;UINT u = 0; uFileCount = ::DragQueryFile(hDropInfo, -1, cFileName, sizeof(cFileName)); // 找出拖曳進入的檔案總數if(uFileCount == 1){::DragQueryFile(hDropInfo, 0, cFileName, sizeof(cFileName)); // 找出第0個檔案的完整路徑與檔名::DragFinish(hDropInfo); }else{::DragFinish(hDropInfo);MessageBox("Please drop single file at one time",0,0);goto enddrop;}//----------------------------------////在此加入自己所需操作Dropflg = 1; // 表示檔案是以拖曳方式開啟之 flagOnBnClickedButtonLoadMpo(); // 其他操作//----------------------------------//enddrop:CDialog::OnDropFiles(hDropInfo);Step 6 : 增加 [ CWnd::DragAcceptFiles(); ] 於該 Dlg 的 OnInitDialog() 中Step 7 : 完成